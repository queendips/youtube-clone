apiVersion: v1
kind: Service
metadata:
  name: youtube-clone-app
  namespace: default
  labels:
    app: youtube-clone-app
spec:
  type: LoadBalancer
  ports:
  - port: 5000
    targetPort: 5000
  selector:
    app: youtube-clone-app
